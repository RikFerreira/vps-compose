server {
    listen 80;
    server_name rikalves.com www.rikalves.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 'ok';
    }
}
